// import { Result } from '@/types/result.types';
// import { Task } from '@/types/task.types';

// type GroupTasks = {
//   username: string;
//   tasks: Task; // 質問文に合わせて単一タスク
// };

/**
 * 指定したgroupIdのグループメンバーと、そのメンバーが担当しているタスクを返す関数
 * 最終的にResult<GroupTasks[]>型を返す
 */
