type UserTabProps = {
  userId: string;
};

export const UserTab = ({ userId }: UserTabProps) => {
  console.log(userId);
  return (
    <div className="">
      <div className=""></div>
    </div>
  );
};
